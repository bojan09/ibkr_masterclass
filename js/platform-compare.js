import { PLATFORM_EQUIVALENTS } from "../data/platform-equivalents.js";

export function renderPlatformCompare(container) {
  container.innerHTML = `<div class="platform-compare"><header class="page-hero"><div><p class="eyebrow">Cross-platform mastery</p><h1>IBKR Desktop and TWS/Mosaic</h1><p>Repeat the same task in both genuine paper applications. Equivalent purpose does not mean identical controls.</p></div><div class="page-hero__stat"><strong>${PLATFORM_EQUIVALENTS.length}</strong><span>Mapped workflows</span></div></header><div class="comparison-table-wrap"><table class="comparison-table"><thead><tr><th>Task</th><th>IBKR Desktop</th><th>TWS / Mosaic</th><th>Important distinction</th></tr></thead><tbody>${PLATFORM_EQUIVALENTS.map((item) => `<tr><th><span class="comparison-kind">${item.kind === "non-equivalent" ? "Different model" : "Equivalent task"}</span>${item.task}</th><td data-label="IBKR Desktop">${item.desktop.location}<a href="#/platforms/desktop/missions/${item.desktop.workflowId}">Desktop walkthrough</a></td><td data-label="TWS / Mosaic">${item.tws.location}<a href="#/platforms/tws/missions/${item.tws.workflowId}">TWS walkthrough</a></td><td data-label="Important distinction">${item.explanation}</td></tr>`).join("")}</tbody></table></div><p class="risk-callout"><strong>Practice rule:</strong> Confirm Paper Trading separately in each application. Account mode does not transfer merely because both applications use the same IBKR account family.</p></div>`;
}
